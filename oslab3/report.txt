1.
	单线程和多线程最大的区别就在于工作的数量和CPU的利用率。单线程程序只能执行一个任务，CPU利用低。多线程程序可以做到多任务并行的动态处理，CPU利用高，但是编程复杂，容易出错。

2.
	修改时间片是在linux-0.11/include/linux/sched.h文件中INIT_TASK中修改，将priority的值改变。修改前后log统计的变化不太大，因为对子进程的分配时间比时间片大很多，修改时间片不会产生太大的影响。
